<UserControl x:Class="WorkoutTracker2.Views.ChartViews.WeightChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">
    <Grid>
        <lvc:CartesianChart Series="{Binding CombinedSeries}">

            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Date" Labels="{Binding DateLabels}" />
            </lvc:CartesianChart.AxisX>

            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Average Weight / Total Reps / Sets" />
            </lvc:CartesianChart.AxisY>

        </lvc:CartesianChart>
    </Grid>

</UserControl>

