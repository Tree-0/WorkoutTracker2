<UserControl x:Class="WorkoutTracker2.DataEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkoutTracker2"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="FloralWhite">
    <Grid x:Name="DataEntryControlGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="2" Grid.RowSpan="2"/>
        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="2" Grid.RowSpan="2"/>
        <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="2" Grid.RowSpan="2"/>
        
        <!-- Date Selector for adding and displaying workouts-->
        <Calendar x:Name="WorkoutCalendar"
                      Grid.Column="0" Grid.Row="0" Grid.RowSpan="1"
                      HorizontalAlignment="Center" VerticalAlignment="Top"
                      Margin="0 30 0 0"
                      Background="MistyRose"
                  SelectedDatesChanged="WorkoutCalendar_SelectedDatesChanged"/>

        <StackPanel x:Name="DataDisplayStackPanel" Grid.Column="0" Grid.Row="1" Grid.RowSpan="1"
                    VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            
            <!-- Textbox displays date selected -->
            <TextBox HorizontalAlignment="Stretch" VerticalAlignment="Top" IsReadOnly="True"
                     Margin="10 15 10 15" Background="White" BorderBrush="Gray" BorderThickness="1"
                     MaxWidth="200"
                     Text="{Binding ElementName=WorkoutCalendar, Path=SelectedDate, StringFormat='{}{0:MM/dd/yyyy}'}"/>
            
            <Label HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" FontWeight="DemiBold"
                   Margin="0 0 0 0" Content="Workout Information:"/>

            <!-- ScrollViewer to contain the RichTextBox and prevent overflow -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                          MaxHeight="200" MaxWidth="200" BorderBrush="Gray" BorderThickness="1"
                          Margin="10 0 10 0">
                <TextBox x:Name="SelectedWorkoutInfoBox" IsReadOnly="True"
                             HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                             BorderBrush="Gray" BorderThickness="1"
                             Margin="0 0 0 15" AcceptsReturn="True"/>
            </ScrollViewer>

        </StackPanel>
        
        
        <!-- Exercise Type selection and addition -->
        <StackPanel Grid.Column="1" Grid.Row="0" x:Name="ExerciseSelectionStackPanel">
            <ComboBox x:Name="ExerciseNameSelectionBox"
                  MaxWidth="200" MaxHeight="20" IsEditable="True"
                  VerticalAlignment="Top" Margin="10 30 10 10"/>
            
            <!-- Add new exercise type -->
            <Button x:Name="AddExerciseTypeButton"
                    MaxWidth="140" Height="30" Margin="10 0 10 10"
                    Content="Add Exercise Type" HorizontalAlignment="Stretch"
                    Click="AddExerciseTypeButton_Click"/>
            <!-- Delete selected exercise type : THIS IS RISKY -->
            <Button x:Name="RemoveExerciseTypeButton"
                    MaxWidth="140" Height="30" Margin="10 0 10 10"
                    Content="Remove Exercise Type" HorizontalAlignment="Stretch"
                    Click="RemoveExerciseTypeButton_Click"/>

        </StackPanel>


        <!-- Data Entry -->
        <DataGrid Grid.Column="2" Grid.Row="0" CanUserAddRows="True" CanUserDeleteRows="True"
                  AddingNewItem="DataGrid_AddingNewItem">
            <DataGrid.Columns>
                <DataGridTextColumn/>
                <DataGridTextColumn/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Buttons for dealing with Data Entry -->
        <Grid Grid.Column="2" Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <!-- Checkbox for barbell vs. dumbell weight entry 
            (Barbell will multiply displayed value by 2) -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <CheckBox x:Name="IsWeightPerLimbCheckBox" VerticalAlignment="Center"/>
                <Label HorizontalAlignment="Stretch" Content="Dumbbells (weight is per limb)"/>
            </StackPanel>
            
            <Button Content="Add Exercise"/>
            <Button Content="Submit Workout"/>
        </Grid>
        


    </Grid>
</UserControl>
